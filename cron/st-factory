# ST Factory Feedback Loop
# Replaces the previous Sky-Lynx-only cron
# Install: sudo cp /home/ubuntu/projects/st-factory/cron/st-factory /etc/cron.d/
# Verify: sudo systemctl restart cron && sudo tail -f /var/log/syslog | grep CRON

# Environment
SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

# Create log directory if needed
# sudo mkdir -p /var/log/st-factory && sudo chown ubuntu:ubuntu /var/log/st-factory

# Run weekly on Sundays at 2 AM
0 2 * * 0 ubuntu /home/ubuntu/projects/st-factory/scripts/run_loop.sh >> /var/log/st-factory/loop.log 2>&1
